<?xml version="1.0" encoding="utf-8"?>
<Include>
<% RubyInstaller.const_get(ENV['RUNTIME_NAME']).wix_config.each do |key, value| %>
  <?define <%= "#{key}=#{value.inspect}" %>  ?> 
<% end %>
</Include>
